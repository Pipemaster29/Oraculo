# A Vercel instala isto na função Python. `requests` só é usado pelos scripts de
# coleta, mas fica aqui porque `oraculo/fontes/` é importado pelo pacote inteiro
# e um import que falha na função é um 500 sem mensagem.
fastapi==0.115.6
jinja2==3.1.5
requests==2.32.3

# Só para rodar local (`uvicorn api.index:app --reload`). A Vercel não usa: ela
# serve o `app` ASGI com o servidor dela. Fica aqui mesmo assim porque o README
# manda rodar esse comando depois de um `pip install -r requirements.txt`, e uma
# instrução que não funciona é pior que instrução nenhuma.
uvicorn==0.34.0
